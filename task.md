// TODO : The current getFullReport returns all documents, which is not a good practice as the database can have millions of documents. We should implement pagination to return only a specific number of documents.

// TODO : Use Custom Response Wrapper (successResponse, errorResponse, PaginationResponse)

// TODO : Use Pagniated Response for /unmatched route as well